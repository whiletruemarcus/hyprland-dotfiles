# ========================================
# LAYOUT CONFIGURATION
# Window layout and tiling settings
# ========================================

# ========================================
# DWINDLE LAYOUT
# ========================================
dwindle {
    preserve_split = true
    smart_split = false
    smart_resizing = false
}

# ========================================
# MASTER LAYOUT
# ========================================
master {
    new_status = master
    mfact = $masterRatio
}